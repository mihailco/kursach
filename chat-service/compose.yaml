services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=postgres'
      - 'POSTGRES_PASSWORD=admin'
      - 'POSTGRES_USER=admin'

    expose:
      - "5433"
    command: -p 5433
    ports:
      - "5433:5433"
